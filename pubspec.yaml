name: recursive_tree_flutter
description: Customize your tree UI with NO LIMITS! By mainly focusing on data structure, you can customize the tree UI with various styles.
version: 2.0.0
publish_to: none # Remove this line if you wish to publish to pub.dev
repository: https://github.com/nvbien2000/recursive_tree_flutter

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  unit_tree_data_structure:
    path: ../unit_tree_data_structure

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: null
